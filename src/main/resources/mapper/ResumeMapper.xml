<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.scau.linyuanbin.recruitment.mapper.ResumeMapper">
    <resultMap id="resumeMap" type="Resume">
        <id column="resumeId" property="resumeId"></id>
        <result column="personId" property="personId"></result>
        <result column="resumeName" property="resumename"></result>
        <result column="selfDescription" property="selfdescription"></result>

        <association property="expectJob" column="resumeId" select=""></association>
        <association property="basicinfo" column="personId" select=""></association>
        <collection property="educationList" column="resumeId" select=""></collection>
        <collection property="projectExperienceList" column="resumeId" select=""></collection>
        <collection property="workExperienceList" column="resumeId" select=""></collection>
    </resultMap>

    <!--根据resumeId获得对象-->

    <!--根据personId获得对象-->

    <!--新增对象-->

    <!--更新对象-->
</mapper>